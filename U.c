// write a c program that accepts two item' weights (floating points value) 
//and number of purchase (floating points values)and calculate the average value of the item's ?
#include<stdio.h>
void main ()
{
    float num1, num2, result;
    printf("Enter the first number :");
    scanf("%f",&num1);
    printf("Enter the second number :");
    scanf("%f",&num2);
    result=num1*num2;
    printf("The multiplicatin value is = %.2f",result);
    // The result will be printed upto 2 decimal points*/

}
















